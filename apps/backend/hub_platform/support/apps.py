from django.apps import AppConfig


class SupportConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    label = "support"
    name = "hub_platform.support"
    verbose_name = "Support department and product identity contracts"
