Small round avatar used at the start of every chat bubble, distinguishing the AI agent (neutral gray, bot glyph) from a human operator (colored initials).

```jsx
<ActorBadge actor="ai" />
<ActorBadge actor="human" initials="АК" color="#1677ff" />
```
