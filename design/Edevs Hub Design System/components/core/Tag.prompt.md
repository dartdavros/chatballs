Small colored label. Two shapes: rectangular `tag` (product/channel labels inside tables) and rounded `pill` (status chips, usually with a `dot`) — color is never the only signal, always pair with text.

```jsx
<Tag tone="success">FirePage</Tag>
<Tag pill tone="success" dot>Нормально</Tag>
```
