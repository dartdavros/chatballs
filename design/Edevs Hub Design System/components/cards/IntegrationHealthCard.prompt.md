A single-row status line for one integration (OpenRouter, Точка, MAX, Telegram, Web Chat, Fulfillment). Stack rows in a white card with hairline top-borders (no per-row card chrome).

```jsx
<IntegrationHealthCard name="Точка" group="Платежи и фискализация" status="degraded" />
```
